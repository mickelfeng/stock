// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("stock", "for stock support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("stock", "enable stock support", "no");

if (PHP_STOCK != "no") {
	EXTENSION("stock", "stock.c");
}

